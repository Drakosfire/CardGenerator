# CardGenerator
From the CLI, this is a prototpying space built to establish the functionality of the concept and build the backend before a UI and frontend. 
Takes user input and generates a collectible card with custom or LLM generated text and image generation
